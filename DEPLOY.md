# راهنمای انتشار — سایت نماد نیرو روی لیارا

این سند مستقل است؛ برای اجرای مراحل نیازی به سابقهٔ گفت‌وگو یا دانش قبلی از پروژه نیست.

---

## ۱. این پروژه چیست

سایت با **Astro ۵** ساخته شده و روی **Node.js** اجرا می‌شود.

- **۴۰ صفحه** در زمان build ساخته می‌شوند و به‌صورت فایل آمادهٔ HTML سرو می‌شوند (سریع، بدون پردازش سرور).
- فقط چند مسیر روی سرور اجرا می‌شوند: `/api/quote` (دریافت فرم)، `/leads` (پنل درخواست‌ها) و چند ریدایرکت آدرس‌های قدیمی وردپرس.
- **دیتابیس ندارد.** محتوا در فایل‌های JSON و Markdown داخل مخزن است.
- **PHP ندارد.** نسخه‌های قبلی داشتند؛ کاملاً حذف شده.

آدرس‌ها بدون پسوند هستند (`/shop`، نه `/shop.html`) — همان شکلی که سایت وردپرسی قبلی داشت، تا لینک‌های ایندکس‌شده در گوگل نشکنند.

---

## ۲. پیش‌نیازها

| مورد | نسخه |
|---|---|
| Node.js | ۲۲ (همان نسخه‌ای که در `liara.json` تنظیم شده) |
| npm | همراه Node |
| Liara CLI | `npm install -g @liara/cli` |

---

## ۳. اجرا روی سیستم خودتان

```bash
npm install
```
```bash
npm run dev
```

برای دیدن دقیقاً همان چیزی که روی سرور اجرا می‌شود:

```bash
npm run build && npm start
```

---

## ۴. ساخت اپ در لیارا

۱. وارد پنل لیارا شوید → **ساخت برنامه جدید**
۲. پلتفرم: **Node.js**
۳. نام اپ: `namadniroo` (اگر نام دیگری انتخاب کردید، مقدار `app` را در `liara.json` عوض کنید)

---

## ۵. ساخت دیسک — این مرحله را رد نکنید

فایل‌سیستم لیارا با **هر انتشار جدید پاک می‌شود**. درخواست‌های ثبت‌شدهٔ مشتریان روی دیسک ذخیره می‌شوند، پس بدون این مرحله با هر دیپلوی از بین می‌روند.

در پنل لیارا → بخش **دیسک‌ها** → دیسکی با این مشخصات بسازید:

| فیلد | مقدار |
|---|---|
| نام | `leads` |
| مسیر اتصال | `/data` |

این مقادیر باید دقیقاً با `disks` در `liara.json` یکی باشند.

---

## ۶. متغیرهای محیطی

مقادیر **غیرمحرمانه** از قبل در `liara.json` تعریف شده‌اند (`HOST`، `PORT`، `LEADS_DIR`) — کاری با آن‌ها ندارید.

مقادیر زیر **محرمانه‌اند** و باید در پنل لیارا (بخش متغیرهای محیطی) تنظیم شوند تا وارد مخزن نشوند:

| متغیر | برای چه | نمونه |
|---|---|---|
| `LEADS_USER` | نام کاربری ورود به `/leads` | `admin` |
| `LEADS_PASS` | رمز ورود به `/leads` | یک رمز قوی |
| `SMTP_HOST` | سرور ایمیل | `smtp.c1.liara.email` |
| `SMTP_PORT` | پورت | `465` |
| `SMTP_USER` | نام کاربری SMTP | از سرویس ایمیل لیارا |
| `SMTP_PASS` | رمز SMTP | از سرویس ایمیل لیارا |
| `MAIL_FROM` | فرستنده | آدرسی که در سرویس ایمیل ثبت شده |
| `MAIL_TO` | گیرندهٔ درخواست‌ها | `sales@namadniroo.ir` |

**نکته‌های مهم:**

- تا وقتی `LEADS_USER` و `LEADS_PASS` تنظیم نشده‌اند، صفحهٔ `/leads` بسته می‌ماند و خطای ۵۰۳ می‌دهد. این عمدی است — پنل هرگز بدون رمز باز نمی‌شود.
- تا وقتی تنظیمات SMTP نیامده، فرم کار می‌کند و درخواست‌ها **ذخیره می‌شوند**، فقط ایمیل ارسال نمی‌شود. یعنی لیدی گم نمی‌شود.
- `MAIL_FROM` حتماً باید آدرسی باشد که در سرویس ایمیل لیارا ثبت شده، وگرنه ارسال رد می‌شود.

---

## ۷. انتشار

```bash
liara login
```
```bash
liara deploy
```

لیارا خودش `npm install` و `npm run build` را اجرا می‌کند و بعد `npm start` را بالا می‌آورد.

---

## ۸. بررسی بعد از اولین انتشار

لیارا یک زیردامنه‌ی رایگان می‌دهد. **همه‌چیز را اول روی آن تست کنید**، پیش از اتصال دامنهٔ اصلی:

- [ ] صفحهٔ اصلی و صفحات داخلی باز می‌شوند
- [ ] `/shop/fronius-primo` و یک مقالهٔ مجله باز می‌شوند
- [ ] `/leads` بدون رمز، رمز می‌خواهد
- [ ] `/leads` با رمز درست باز می‌شود
- [ ] یک درخواست تستی از فرم بفرستید → باید در `/leads` ظاهر شود
- [ ] اگر SMTP تنظیم شده، ایمیل هم برسد
- [ ] `/nonexistent` صفحهٔ ۴۰۴ اختصاصی را نشان دهد

**تست ماندگاری دیسک:** بعد از ثبت یک درخواست تستی، یک‌بار دوباره `liara deploy` بزنید و ببینید آن درخواست هنوز در `/leads` هست. اگر نبود، دیسک درست وصل نشده است.

---

## ۹. اتصال دامنه — آخرین مرحله

فقط وقتی همهٔ موارد بالا تأیید شد:

۱. در پنل لیارا دامنه `namadniroo.ir` را به اپ اضافه کنید
۲. رکوردهای DNS را طبق راهنمای لیارا تنظیم کنید
۳. منتظر صدور خودکار گواهی SSL بمانید
۴. بعد از انتقال، سایت را از چند دستگاه و شبکهٔ متفاوت باز کنید

> ⚠️ سایت وردپرسی فعلی روی هاست دیگری بالاست. **پیش از تغییر DNS، از فایل‌ها و دیتابیس آن بک‌آپ کامل بگیرید و نگه دارید** — تنها راه بازگشت همین است.

---

## ۱۰. انتشار به‌روزرسانی‌های بعدی

```bash
git pull && liara deploy
```

محتوا (پروژه‌ها، محصولات، مقالات) در `src/data/` و `src/content/` است. بعد از ویرایش، دوباره deploy کنید.
