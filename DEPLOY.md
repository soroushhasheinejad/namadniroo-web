> ## ⚠️ این راهنما منسوخ شده است
>
> این متن برای زمانی نوشته شد که سایت قرار بود روی هاست اشتراکی **نت‌افراز**
> (آپاچی + PHP + FTP) منتشر شود.
>
> از آن زمان دو چیز عوض شده:
> - PHP کاملاً از پروژه حذف شد؛ فرم و پنل لیدها حالا داخل خود Astro اجرا می‌شوند.
> - مقصد انتشار به **لیارا** (پلتفرم Node.js) تغییر کرده است.
>
> بنابراین مراحل FTP و cPanel در این فایل دیگر معتبر نیستند.
> راهنمای به‌روز در `README.md` است و راهنمای کامل لیارا در مرحلهٔ بعد نوشته می‌شود.

---

# راهنمای انتشار سایت نماد نیرو

این سند برای کسی نوشته شده که می‌خواهد این سایت را روی هاست بالا بیاورد و
**جایگزین سایت وردپرسی فعلی `namadniroo.ir`** کند. فرض بر این است که با
گفت‌وگوهای قبلی آشنا نیستید؛ هر چیزی که لازم است اینجا آمده.

---

## ۱. این پروژه چیست؟

یک سایت **استاتیک** ساخته‌شده با [Astro](https://astro.build) که جایگزین سایت
وردپرسی فعلی می‌شود.

- خروجی نهایی، فایل‌های `.html` ساده است — **نه وردپرس، نه دیتابیس MySQL، نه PHP برای رندر صفحات**.
- فقط سه فایل PHP کوچک برای فرم تماس و پنل مشاهدهٔ درخواست‌ها وجود دارد.
- آدرس‌ها به شکل `/shop.html` هستند (تنظیم `build.format: 'file'` در `astro.config.mjs`).

### نیازمندی‌های هاست

| مورد | توضیح |
|---|---|
| Apache | فایل `.htaccess` استفاده می‌شود؛ ماژول‌های `mod_rewrite`، `mod_headers`، `mod_expires`، `mod_deflate`، `mod_alias` |
| PHP ۷٫۴ یا بالاتر | برای فرم تماس |
| اکستنشن `pdo_sqlite` | برای ذخیرهٔ درخواست‌ها — **معمولاً پیش‌فرض فعال است، ولی حتماً چک کنید** |
| تابع `mail()` | برای ارسال ایمیل درخواست‌ها |

هاست اشتراکی معمولی (cPanel/DirectAdmin) همهٔ این‌ها را دارد. Node.js **روی هاست لازم نیست** —
ساخت سایت روی سیستم شما یا روی گیت‌هاب انجام می‌شود، نه روی سرور.

---

## ۲. اجرا روی سیستم خودتان

نیاز به **Node.js نسخهٔ ۲۰ یا بالاتر**.

```bash
npm install
npm run dev        # http://localhost:4321
```

ساخت خروجی نهایی:

```bash
npm run build      # خروجی در پوشهٔ dist/
npm run preview    # پیش‌نمایش خروجی نهایی
```

هرچه در `dist/` است، دقیقاً همان چیزی است که باید روی هاست برود.

---

## ۳. دو راه برای انتشار

### راه ۱ — خودکار با GitHub Actions (پیشنهادی)

فایل `.github/workflows/deploy.yml` از قبل آماده است. با هر `push` روی شاخهٔ `main`،
گیت‌هاب سایت را می‌سازد و با FTP روی هاست آپلود می‌کند.

برای فعال شدن، این چهار مقدار باید در
`Settings → Secrets and variables → Actions` مخزن تعریف شوند:

| نام Secret | مقدار نمونه |
|---|---|
| `FTP_SERVER` | `ftp.namadniroo.ir` |
| `FTP_USERNAME` | نام کاربری اکانت FTP |
| `FTP_PASSWORD` | رمز همان اکانت |
| `FTP_DIR` | `/public_html/` |

> **توصیهٔ امنیتی:** یک اکانت FTP **جداگانه** فقط برای دیپلوی بسازید که دسترسی‌اش
> محدود به پوشهٔ سایت باشد — نه اکانت اصلی هاست.

نکتهٔ فنی: این اکشن یک فایل وضعیت روی سرور نگه می‌دارد و فقط فایل‌هایی را مدیریت
می‌کند که خودش آپلود کرده. بنابراین فایل‌هایی که سمت سرور ساخته می‌شوند
(مثل `.htpasswd` محافظت پوشهٔ `leads` یا فایل دیتابیس درخواست‌ها) دست‌نخورده می‌مانند.

### راه ۲ — آپلود دستی

```bash
npm run build
```

سپس **محتوای داخل** پوشهٔ `dist/` (نه خود پوشه) را در ریشهٔ هاست
(`public_html` یا معادلش) آپلود کنید.

⚠️ حواستان باشد فایل‌های مخفی مثل `.htaccess` هم آپلود شوند — بعضی برنامه‌های FTP
به‌صورت پیش‌فرض فایل‌های نقطه‌دار را نشان نمی‌دهند.

---

## ۴. کارهای لازم بعد از اولین انتشار

این سه مورد **حتماً** باید انجام شوند:

### ۴-۱. ساخت ایمیل `sales@namadniroo.ir`
درخواست‌های فرم به این آدرس می‌روند. اگر بخواهید آدرس دیگری باشد، مقدار `$to` در
بالای `public/send-quote.php` را عوض کنید.

### ۴-۲. رمزگذاری پوشهٔ `/leads` ⚠️ مهم
صفحهٔ `namadniroo.ir/leads/` همهٔ درخواست‌های ثبت‌شده (نام و شمارهٔ تماس مشتریان) را
نشان می‌دهد و **خودش هیچ رمزی ندارد**.

از پنل هاست (cPanel → **Directory Privacy**) پوشهٔ `leads` را با نام کاربری و رمز
محافظت کنید. بدون این کار، اطلاعات تماس مشتریان برای همه قابل مشاهده است.

### ۴-۳. تست واقعی فرم
یک درخواست تستی از `namadniroo.ir/contact.html` ثبت کنید و مطمئن شوید:
- در `namadniroo.ir/leads/` نمایش داده می‌شود، **و**
- ایمیلش به `sales@namadniroo.ir` می‌رسد (پوشهٔ اسپم را هم چک کنید).

اگر ایمیل نرسید ولی رکورد ثبت شد، مشکل از `mail()` هاست است نه از سایت —
رکورد در ستون «ایمیل» با برچسب «ارسال نشد» مشخص می‌شود.
در این حالت رکورد **گم نشده**؛ فقط باید تنظیمات ایمیل هاست (معمولاً SPF دامنه) بررسی شود.

---

## ۵. جایگزینی سایت وردپرسی فعلی

⚠️ **قبل از هر کاری، بک‌آپ کامل بگیرید** — هم فایل‌های وردپرس، هم دیتابیس MySQL.
(cPanel → Backup / JetBackup، یا دستی با File Manager + phpMyAdmin export.)

### مسیر پیشنهادی، مرحله‌به‌مرحله

1. **اول روی یک زیردامنه تست کنید.** مثلاً `new.namadniroo.ir` بسازید و سایت را
   آنجا بالا بیاورید. سایت اصلی دست‌نخورده می‌ماند.
2. همهٔ صفحات را چک کنید: صفحهٔ اصلی، فروشگاه و صفحات تک‌محصول، پروژه‌ها،
   مجله، تماس، سرمایه‌گذاری، AE Solar — و فرم (طبق بند ۴-۳).
3. حالت موبایل را هم ببینید.
4. بک‌آپ بگیرید (بالا).
5. فایل‌های وردپرس را از `public_html` بردارید و محتوای `dist/` را جایش بگذارید.
6. بعد از سوییچ، سایت را از چند دستگاه/شبکهٔ متفاوت باز کنید و SSL را چک کنید.

### دربارهٔ حفظ رتبهٔ گوگل — این بخش را جدی بگیرید

سایت فعلی سال‌هاست ایندکس شده. فایل `public/.htaccess` از قبل شامل ریدایرکت ۳۰۱
برای مسیرهای قدیمی وردپرس/ووکامرس است:

- `/contact-us` → `/contact.html`
- `/product/...`، `/product-category/...` → `/shop.html`
- `/category/...`، `/tag/...`، آرشیوهای تاریخ‌دار → `/magazine.html`
- آدرس‌های بدون پسوند (مثل `/shop`) → نسخهٔ `.html`
- `www` → بدون `www`، و اجبار HTTPS

**اما یک کار ناتمام هست:** ریدایرکت صفحات تک‌محصول فعلاً «دسته‌جمعی» است — یعنی همهٔ
`/product/<اسلاگ>/`ها به فهرست فروشگاه می‌روند، نه به صفحهٔ همان محصول در سایت جدید.
این از ۴۰۴ خیلی بهتر است، ولی ایده‌آل نیست.

اگر می‌خواهید کامل باشد: قبل از خاموش‌کردن وردپرس، فهرست آدرس‌های فعلی را از
`https://www.namadniroo.ir/wp-sitemap.xml` یا از Google Search Console بگیرید و
در بخش ۳ فایل `.htaccess` نگاشت تک‌به‌تک اضافه کنید:

```apache
Redirect 301 /product/<اسلاگ-قدیمی>/ /shop/<slug-جدید>.html
```

### بعد از سوییچ

- `sitemap-index.xml` را در Google Search Console ثبت کنید.
- چند آدرس قدیمی را با `curl -I https://namadniroo.ir/<آدرس-قدیمی>` تست کنید و
  مطمئن شوید `301` برمی‌گردانند نه `404`.

---

## ۶. ویرایش محتوا

محتوا در فایل‌های متنی است؛ بعد از هر تغییر باید `npm run build` اجرا و دوباره
منتشر شود (یا اگر انتشار خودکار فعال باشد، فقط `git push`).

| چه چیزی | کجا |
|---|---|
| محصولات | `src/data/products.json` |
| پروژه‌ها | `src/data/projects.json` |
| مقالات مجله | `src/content/articles/*.md` |
| منو، آمار، برندها، متن‌های ثابت | `src/data/site.ts` |
| محتوای صفحهٔ AE Solar | `src/data/aesolar.ts` |
| تصاویر | `src/assets/` (خودکار بهینه و به WebP تبدیل می‌شوند) |

جزئیات بیشتر در `README.md`.

---

## ۷. کارهای ناتمام (تا امروز)

برای شفافیت، این‌ها هنوز انجام نشده‌اند:

1. **مقالات مجله خالی‌اند.** هر ۱۴ مقاله عنوان و تاریخ واقعی دارند ولی متن هرکدام
   فقط یک خط «به‌زودی منتشر می‌شود» است. ساختارش کامل است، فقط متن لازم دارد.
2. **متن صفحات محصول عمومی است** — یک پاراگراف مشترک که فقط دستهٔ محصول در آن عوض می‌شود.
3. **نگاشت تک‌به‌تک ریدایرکت محصولات** (بند ۵ بالا).
4. **عکس ۹ محصول از ۱۶ تا موجود نیست** (برندهای Deye، Goodwe، Sineng، Trina) — تا
   وقتی عکس اضافه نشود، به‌جای تصویر نام برند نمایش داده می‌شود که رفتار عمدی و
   بی‌اشکالی است.
5. **پنل مدیریت محتوا و بخش کدهای رهگیری (Google Analytics / Ads) هنوز ساخته نشده.**

هیچ‌کدام مانع بالا آمدن سایت نیستند.
