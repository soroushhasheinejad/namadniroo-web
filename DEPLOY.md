# راهنمای انتشار — سایت نماد نیرو روی لیارا

این سند مستقل است؛ برای اجرای مراحل نیازی به سابقهٔ گفت‌وگو یا دانش قبلی از پروژه نیست.

---

## ۱. این پروژه چیست

سایت با **Astro ۵** ساخته شده و روی **Node.js** اجرا می‌شود. دیتابیسش SQLite است — یک فایل روی دیسک پایدار، بدون سرویس جداگانه و بدون هزینهٔ ماهانهٔ اضافه.

- صفحات معرفی شرکت (درباره ما، تماس، AE Solar، حوزه‌های فعالیت) در زمان build ساخته می‌شوند.
- صفحات محتوایی (خانه، فروشگاه، پروژه‌ها، مجله) روی سرور رندر می‌شوند تا تغییرات پنل بدون انتشار دوباره دیده شوند. یک لایهٔ کش باعث می‌شود این کار عملاً هزینه‌ای نداشته باشد.
- **پنل مدیریت** روی `/admin` است: محصولات، پروژه‌ها، مقالات، تصاویر، متن‌های سایت، سئو و درخواست‌های مشتریان.
- **PHP ندارد.** نسخه‌های قبلی داشتند؛ کاملاً حذف شده.

آدرس‌ها بدون پسوند هستند (`/shop`، نه `/shop.html`) — همان شکلی که سایت وردپرسی قبلی داشت، تا لینک‌های ایندکس‌شده در گوگل نشکنند.

---

## ۲. پیش‌نیازها

| مورد | نسخه |
|---|---|
| Node.js | ۲۲ (همان نسخه‌ای که در `liara.json` تنظیم شده) |
| Liara CLI | `npm install -g @liara/cli` |

---

## ۳. اجرا روی سیستم خودتان

**هیچ نصبی جز Node لازم نیست.** دیتابیس یک فایل SQLite در پوشهٔ `.data/` است که خودکار ساخته می‌شود. محیط توسعه و سرور دقیقاً یک موتور را اجرا می‌کنند؛ فقط مسیر فایل فرق می‌کند.

```bash
npm install
npm run db:seed                                    # انتقال محتوا به دیتابیس (فقط بار اول)
npm run db:admin -- you@namadniroo.ir "نام شما"    # ساخت کاربر پنل؛ رمز چاپ می‌شود
npm run dev                                        # http://localhost:4321
```

برای دیدن دقیقاً همان چیزی که روی سرور اجرا می‌شود:

```bash
npm run build && npm start
```

---

## ۴. ساخت اپ در لیارا

پنل لیارا → **ساخت برنامه جدید** → پلتفرم **Node.js** → نام: `namadniroo`
(اگر نام دیگری انتخاب کردید، مقدار `app` را در `liara.json` عوض کنید)

سرویس دیتابیس جداگانه لازم نیست.

---

## ۵. ساخت دیسک — این مرحله را رد نکنید

فایل‌سیستم لیارا با **هر انتشار جدید پاک می‌شود**. هم دیتابیس و هم تصاویر روی دیسک می‌نشینند، پس بدون این مرحله همهٔ محتوا و همهٔ درخواست‌های مشتریان با اولین دیپلوی بعدی از بین می‌روند.

پنل لیارا → **دیسک‌ها** → دیسکی با نام `data` بسازید (۵ گیگابایت کافی است).

مسیر اتصال (`/data`) از قبل در `liara.json` تعریف شده است. دیتابیس در `/data/namadniroo.db` و تصاویر در `/data/media/` ذخیره می‌شوند.

---

## ۶. متغیرهای محیطی

در پنل لیارا → **تنظیمات** → **متغیرهای محیطی**. این مقادیر هرگز داخل مخزن قرار نمی‌گیرند.

| متغیر | لازم؟ | کاربرد |
|---|---|---|
| `SMTP_HOST` | برای ایمیل | سرور ارسال ایمیل |
| `SMTP_PORT` | برای ایمیل | معمولاً ۴۶۵ |
| `SMTP_USER` | برای ایمیل | نام کاربری |
| `SMTP_PASS` | برای ایمیل | رمز |
| `MAIL_TO` | خیر | گیرندهٔ درخواست‌ها (پیش‌فرض `sales@namadniroo.ir`) |
| `MAIL_FROM` | خیر | فرستنده (پیش‌فرض همان `SMTP_USER`) |

`HOST`، `PORT`، `DATABASE_PATH` و `MEDIA_DIR` در `liara.json` تعریف شده‌اند و لازم نیست دستی وارد شوند.

اگر SMTP تنظیم نشده باشد، فرم کار می‌کند و درخواست‌ها **ذخیره و در صف ایمیل نگه داشته می‌شوند**؛ به‌محض تنظیم SMTP، همان‌ها ارسال می‌گردند. هیچ درخواستی گم نمی‌شود.

---

## ۷. انتشار

انتشار خودکار است: با هر `git push` روی `main`، گیت‌هاب اکشن بیلد می‌گیرد و روی لیارا منتشر می‌کند.

برای این کار یک بار توکن لیارا را در گیت‌هاب تعریف کنید:
**Settings → Secrets and variables → Actions → New repository secret**
با نام `LIARA_API_TOKEN`.

> دلیل استفاده از گیت‌هاب به‌جای `liara deploy` روی سیستم شخصی: آپلود از ایران کند است و اتصال وسط کار قطع می‌شود.

انتشار دستی، اگر لازم شد:

```bash
liara deploy
```

> آپلود مستقیم از ایران گاهی وسط کار قطع می‌شود («Upload failed»). دوباره اجرا کنید، یا از همان مسیر گیت‌هاب استفاده کنید که این محدودیت را ندارد.

**مایگریشن دیتابیس خودکار است.** با اولین درخواست بعد از انتشار، ساختار دیتابیس بررسی و در صورت نیاز به‌روز می‌شود. کاری لازم نیست.

**دربارهٔ health check لیارا:** عمداً در `liara.json` تعریف نشده. روی این اپ، نسخه‌های کاملاً سالم را هم ناسالم اعلام می‌کرد و برمی‌گرداند — حتی وقتی بررسی به مسیری اشاره داشت که به هیچ چیز جز خود پروسه وابسته نبود. به‌جایش، گیت‌هاب اکشن بعد از انتشار `/health` را چک می‌کند و اگر سالم نبود قرمز می‌شود.

دو مسیر برای بررسی دستی:
- `/ping` — فقط می‌گوید پروسه بالاست
- `/health` — دیتابیس و دسترسی به دیسک را هم بررسی می‌کند

---

## ۸. راه‌اندازی اولیه — فقط یک بار

بعد از اولین انتشار موفق، محتوا و کاربر ادمین را بسازید. چون دیتابیس یک فایل روی دیسک سرور است، این دستورها **داخل خود سرور** اجرا می‌شوند:

```bash
liara shell --app namadniroo
# و داخل سرور:
npx tsx scripts/seed.ts
npx tsx scripts/create-admin.ts admin@namadniroo.ir "نام مدیر" admin
```

`db:seed` محصولات، پروژه‌ها، مقالات، تصاویر و متن‌های سایت را منتقل می‌کند. اجرای دوباره‌اش بی‌خطر است، **ولی بعد از شروع کار با پنل دیگر اجرایش نکنید** — محتوای ویرایش‌شده را به نسخهٔ اولیه برمی‌گرداند.

`db:admin` رمز را یک بار چاپ می‌کند. همان را برای ورود به `/admin` استفاده کنید. برای افزودن کاربر یا عوض کردن رمز، دوباره همین دستور را با همان ایمیل بزنید.

نقش‌ها: `admin` (همه‌چیز)، `editor` (فقط محتوا)، `sales` (فقط درخواست‌ها).

---

## ۹. بررسی بعد از اولین انتشار

لیارا یک زیردامنهٔ رایگان می‌دهد. **همه‌چیز را اول روی آن تست کنید**، پیش از اتصال دامنهٔ اصلی:

- [ ] `/health` پاسخ `{"status":"ok"}` بدهد — اگر `degraded` بود، پیام داخلش می‌گوید دیتابیس مشکل دارد یا دیسک
- [ ] صفحهٔ اصلی، فروشگاه، پروژه‌ها و مجله باز شوند و محتوا داشته باشند
- [ ] `/shop/fronius-primo` و یک مقالهٔ مجله باز شوند
- [ ] `/admin` بدون ورود، به صفحهٔ ورود منتقل شود
- [ ] ورود با ایمیل و رمز کار کند
- [ ] در پنل، نام یک محصول را عوض کنید و ذخیره بزنید → **بلافاصله** در سایت دیده شود
- [ ] یک تصویر آپلود کنید → در کتابخانه دیده شود و در فرم محصول قابل انتخاب باشد
- [ ] یک درخواست تستی از فرم سایت بفرستید → در `/admin/leads` ظاهر شود
- [ ] اگر SMTP تنظیم شده، ایمیل هم برسد (تا یک دقیقه طول می‌کشد؛ ارسال از صف انجام می‌شود)
- [ ] `/sitemap.xml` باز شود و مقالات و محصولات را داشته باشد
- [ ] `/nonexistent` صفحهٔ ۴۰۴ اختصاصی را نشان دهد
- [ ] بعد از چند بازدید، `/admin/analytics` عدد نشان دهد (بازدید خودتان از پنل شمرده نمی‌شود، فقط صفحات سایت)
- [ ] درخواست تستی را در `/admin/leads/<id>` باز کنید — باید مسیر بازدیدتان پیش از فرم را نشان دهد

**تست انتساب کانال:** یک بار سایت را با نشانی
`https://<دامنه>/?utm_source=instagram&utm_medium=social&utm_campaign=test` باز کنید، بعد یک درخواست
تستی بفرستید. در پروندهٔ آن لید باید «کانال ورود: شبکه‌های اجتماعی» و همین کمپین دیده شود. اگر
دیده نشد، یعنی کوکی به مرورگر نرسیده — معمولاً نشانهٔ این است که سایت پشت کشی نشسته که کوکی را
حذف می‌کند.

**تست ماندگاری:** بعد از آپلود یک تصویر و ثبت یک درخواست، یک‌بار دوباره دیپلوی کنید و ببینید هر دو سر جایشان هستند. اگر هرکدام رفته، دیسک `data` درست وصل نشده است.

---

## ۱۰. اتصال دامنه

پنل لیارا → **دامنه‌ها** → `namadniroo.ir` را اضافه کنید و رکوردهایی که نشان می‌دهد را در تنظیمات DNS دامنه ثبت کنید. گواهی SSL خودکار صادر می‌شود.

بعد از اتصال دامنه، دوباره `/health` و یک فرم در پنل را تست کنید.

---

## ۱۱. بکاپ — مهم

SQLite بکاپ خودکار ندارد؛ این تنها کاری است که در ازای نداشتن هزینهٔ ماهانه باید دستی انجام شود.

```bash
liara shell --app namadniroo
# داخل سرور:
npx tsx scripts/backup.ts /data/backups
```

خروجی یک فایل کامل و سالم است، حتی اگر سایت در همان لحظه در حال کار باشد (از `VACUUM INTO` استفاده می‌شود، نه کپی ساده). فایل را از سرور بیرون بکشید و جای دیگری نگه دارید — بکاپی که روی همان دیسک بماند در برابر خرابی دیسک بی‌فایده است.

**پیشنهاد:** ماهی یک بار، و حتماً پیش از هر تغییر بزرگ.

بازیابی: فایل بکاپ را به‌جای `/data/namadniroo.db` بگذارید و اپ را ری‌استارت کنید.

---

## ۱۲. آنالیتیکس — چند نکتهٔ عملیاتی

- **حجم دیتابیس.** هر بازدید یک ردیف است. با ترافیک این سایت، رشد ماهانه چند مگابایت
  می‌ماند. رویدادهای خام بعد از ۱۲۰ روز خودکار پاک می‌شوند و خلاصهٔ روزانه‌شان (`daily_stats`)
  برای همیشه می‌ماند، پس مقایسهٔ سال‌به‌سال از دست نمی‌رود.
- **worker.** جمع‌بندی، پاک‌سازی و هشدارهای پیگیری را یک worker داخل خود اپ هر شش ساعت
  انجام می‌دهد؛ چیزی جدا راه‌اندازی نمی‌شود. اگر اپ مدتی خاموش بوده، اولین اجرا روزهای
  جامانده را (تا ۴۰ روز در هر دور) جمع می‌بندد.
- **اجرای دستی:** `npx tsx scripts/rollup.ts` داخل `liara shell`.
- **هشدارها به `MAIL_TO` می‌روند.** اگر SMTP تنظیم نشده باشد، هشدارها در صف می‌مانند و
  در داشبورد پنل به‌صورت «ایمیل ارسال‌نشده» دیده می‌شوند — یعنی چیزی گم نمی‌شود.
- **بکاپ.** جدول‌های `events`، `visitors`، `daily_stats` و `lead_activity` داخل همان فایل
  دیتابیس‌اند، پس بکاپ بخش ۱۱ همه را با هم می‌گیرد.

---

## ۱۳. عیب‌یابی

| نشانه | علت محتمل |
|---|---|
| `/health` می‌گوید `database` خطا دارد | دیسک `data` وصل نشده یا مسیرش قابل نوشتن نیست |
| `/health` می‌گوید `media` در دسترس نیست | همان دیسک `data` — بررسی کنید ساخته و وصل شده باشد |
| صفحات خالی‌اند ولی خطا نمی‌دهند | `npm run db:seed` اجرا نشده |
| فرم‌های پنل خطای ۴۰۳ می‌دهند | دامنه در `security.allowedDomains` داخل `astro.config.mjs` نیست |
| تصاویر یا محتوا بعد از دیپلوی ناپدید می‌شوند | دیسک وصل نشده و همه‌چیز روی فضای موقت رفته است |
| ایمیل نمی‌رسد ولی درخواست‌ها ثبت می‌شوند | تنظیمات SMTP — وضعیت صف در داشبورد پنل دیده می‌شود |

لاگ‌ها: پنل لیارا → بخش **لاگ‌ها**.
