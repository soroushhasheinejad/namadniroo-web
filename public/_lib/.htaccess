# هیچ‌چیز داخل این پوشه نباید مستقیم از مرورگر قابل دسترسی باشد.
# (PHP از سمت سرور همچنان می‌تواند include کند.)

# Apache 2.4
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>

# Apache 2.2
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
