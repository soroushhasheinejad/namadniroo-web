/**
 * متن‌های ثابت سایت.
 *
 * تا پیش از این، تصاویر اینجا با `import` وارد می‌شدند تا Astro در زمان
 * بیلد بهینه‌شان کند. حالا همهٔ تصاویر — از پوستر اسلایدر تا عکس محصول —
 * از کتابخانهٔ رسانه می‌آیند و از پنل قابل تعویض‌اند، پس این فایل دیگر
 * تصویری نمی‌شناسد.
 *
 * صفحات همچنان از همین‌جا می‌خوانند؛ محتوا در `siteContent.ts` است تا
 * اسکریپت‌های خط فرمان (که باندلر Astro را ندارند) هم بتوانند بخوانندش.
 *
 * این مقادیر «پیش‌فرض» هستند: هر کلیدی که در پنل ذخیره شود، نسخهٔ دیتابیس
 * جایش را می‌گیرد.
 */
export * from './siteContent';
