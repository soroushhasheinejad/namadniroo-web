import type { APIRoute } from 'astro';

export const prerender = false;

/**
 * بررسی زنده‌بودن پروسه — سبک‌ترین پاسخ ممکن.
 *
 * میزبان از این مسیر استفاده می‌کند تا تصمیم بگیرد نسخهٔ تازه را منتشر کند
 * یا برگرداند. عمداً به هیچ چیزی جز خود پروسه وابسته نیست: نه دیتابیس، نه
 * دیسک.
 *
 * دلیلش تجربه است. وقتی این بررسی به دیتابیس وصل بود، هنگام انتشار —
 * جایی که کانتینر قدیمی و جدید مدتی هم‌زمان همان فایل SQLite را باز دارند —
 * گاهی کند پاسخ می‌داد و نسخهٔ کاملاً سالم برگردانده می‌شد.
 *
 * برای دیدن وضعیت واقعی سرویس‌ها `/health` هست که دیتابیس و محل ذخیرهٔ
 * تصاویر را هم بررسی می‌کند.
 */
export const GET: APIRoute = () =>
  new Response('ok', {
    headers: { 'Content-Type': 'text/plain; charset=utf-8', 'Cache-Control': 'no-store' },
  });
